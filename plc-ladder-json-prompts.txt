1. CODESYS 三泵水箱自动补水 + 轮换控制(three-pump-control-ladder.json)

实现一个在 CODESYS V3 上运行的三台补水泵自动控制与轮换逻辑（Pump1、Pump2、Pump3），包含系统启停、自保持、液位高/低/超高判断、急停与过载联锁、最小运行时间和主备轮换策略等。

请以 **json 文本** 的方式，输出符合 IEC 61131-3 标准的 **梯形图（Ladder Diagram）控制逻辑结构**，用于后续渲染成梯形图，而不是生成 ST 代码。需要在 json 中明确：I/O 点（输入/输出标签）、内部位和定时器、各梯级（rung）的名称以及触点/线圈的顺序与类型（NO/NC、LATCH、SET/RESET 等）。

---

2. CODESYS 双水泵交替运行控制(dual-pump-water-supply-system.json)

实现一个在 CODESYS V3 上运行的双泵供水系统控制逻辑，支持：系统启停、急停联锁、单泵过载保护、两泵交替运行（一次用泵1，下次用泵2），水箱液位高/低控制启停，以及故障跳过某台泵的逻辑。

要求以 **json 文本** 输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染成梯形图。json 中需包含：输入/输出信号列表、内部轮换状态位、各梯级的条件（启停自保持、轮换选择、过载联锁、液位控制等），以及对应的触点/线圈类型和排列。不要生成具体 PLC 代码，只生成可视化用的梯形图描述。

---

3. CODESYS 恒压供水单泵 PID 控制(constant_pressure_water_supply.json)

实现一个在 CODESYS V3 上运行的恒压供水系统，控制一台变频水泵：系统启停、自保持、急停联锁、过载保护，基于压力反馈和设定值的 PID 控制输出频率指令，以及故障报警与复位逻辑。

请以 **json 文本** 的方式，描述 IEC 61131-3 梯形图的结构，用于后续渲染梯形图。json 中应该包括：I/O 标签（按钮、急停、压力反馈、过载、模拟量输出等），内部 PID 相关位（例如运行允许位、故障位、报警位），以及各梯级的触点/线圈组合，说明 PID 运行启停条件、故障联锁与报警复位。无需给出 PID 算法细节代码，只需梯形图级别的控制逻辑。

---

4. CODESYS 双工位分拣输送线控制（sorting_system_ladder.json）

实现一条主输送线 + 左右两个工位的分拣控制逻辑：系统启停、急停联锁、主输送线运行与到位暂停、根据外部选择信号（Select_Left）决定分拣方向，通过气缸推杆分拣到左/右工位，气缸伸出/缩回到位反馈，故障与报警逻辑，以及左右工位计数功能。

要求以 **json 文本** 输出 IEC 61131-3 梯形图结构，用于后续渲染。json 需包含：所有输入/输出点（启动、停止、急停、光电、气缸反馈、选择信号等），内部请求位（Sort_Request）、计数器标签（Count_Left/Count_Right）以及每个梯级的触点/线圈配置（例如输送线自保持、分拣请求置位、气缸动作控制、计数逻辑、报警联锁等）。

---

5. CODESYS 轨道小车自动/手动双模式控制

实现一个轨道小车（Transfer Cart）的双模式控制：自动模式下在左/右端之间按节拍往返；手动模式下通过 Jog 信号点动前后移动；包含系统启停、急停联锁、左右端到位检测、驱动过载保护、行程超时报警，以及自动/手动模式切换互锁。

请以 **json 文本** 的方式，输出用于 IEC 61131-3 梯形图渲染的逻辑结构。json 中应包括：所有输入信号（模式开关、启停按钮、端位开关、过载、JOG 命令等）、输出信号（Cart_Move_Left/Right、Alarm）、内部状态位（例如 Cart_Run、Auto_Mode、Manual_Mode、Fault）、以及每个梯级的条件和触点/线圈排列，用于表示启停自保持、模式切换、运动方向控制、故障联锁与报警复位。不要输出 ST 或 IL 代码，只给结构化的梯形图描述。

---

6. CODESYS 配料罐加热 + 搅拌工艺控制

实现一个配料罐的自动工艺控制：液位达到工艺要求后，开启加热器和搅拌电机；按温度设定值控制加热启停；存在防超温逻辑（超过上限温度强制停机并报警）；在达到设定温度后持续搅拌一段设定时间；包含系统启停、急停联锁、加热和搅拌过载保护、报警与复位逻辑。

请以 **json 文本** 描述符合 IEC 61131-3 的梯形图控制逻辑，用于后续渲染成梯形图。json 需包含：输入点（启停、复位、急停、液位、温度反馈、过载信号等）、输出点（Heater_Enable、Mixer_Run、Alarm）、内部阶段/定时器位（加热阶段、搅拌阶段、防超温状态等），以及每个梯级的触点与线圈配置，用来表达工艺顺序、温度与液位条件、故障联锁和报警复位。只需结构，不需具体执行代码。

---

7. CODESYS 四电机顺序启动控制

实现一个四台电机（M1~M4）的顺序启动控制系统：在系统启动后按顺序 M1→M2→M3→M4 依次启动，每台之间有固定延时；任何时刻按下停止或发生急停/过载故障时立即停机并锁存故障；提供复位按钮清除故障后可重新启动整个顺序。

要求以 **json 文本** 输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染。json 应列出：所有输入信号（Start/Stop/Reset/Estop/各电机过载等）、输出信号（M1_Run~M4_Run、Alarm）、内部顺序状态位或步进位（例如 Step_1~Step_4）、定时器标签，以及每个梯级的触点/线圈组合，用来表示自保持启停、按步推进顺序启动、过载联锁和报警复位。不要生成具体 PLC 程序，只生成梯形图结构描述。

---

8. CODESYS 单路口三色交通灯定时控制

实现一个单路口三色交通灯控制逻辑：绿灯→黄灯→红灯按固定周期循环；系统启停和急停联锁；通过定时器控制每个灯的亮灯时长，并在周期结束后重新回到绿灯阶段。

请以 **json 文本** 的方式，输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染成梯形图。json 中应包含：输入信号（Start/Stop/Reset/Estop）、输出信号（Red_Lamp、Yellow_Lamp、Green_Lamp）、定时器标签（T_Green、T_Yellow、T_Red），以及每个梯级的触点和线圈（启停自保持、各灯阶段的点亮条件、定时器启停、周期复位逻辑等）。只需结构化描述，不需要具体代码实现。

---

9. CODESYS 三泵污水提升泵站自动控制 + 轮换

实现一个污水提升泵站控制系统，包含 3 台提升泵（Pump1~Pump3）：根据液位低/高/超高信号控制启停；高液位时启动主泵；超高液位时启动第二台泵并报警；具有主泵轮换（按时间或运行周期轮换 Pump1/Pump2/Pump3）、单泵过载故障跳过、系统启停和急停联锁、故障报警与复位逻辑。

请以 **json 文本** 输出符合 IEC 61131-3 的梯形图结构，用于后续渲染。json 需列出：输入信号（Start/Stop/Reset/Estop/Level_Low/High/HH 以及 Pump1_OL~Pump3_OL）、输出信号（Pump1_Run~Pump3_Run、Alarm）、内部轮换状态位（Current_Lead_Pump）、定时器（Min_Run_Time、Rotation_Interval），以及每个梯级的触点和线圈配置，用来表达液位条件启停、轮换选择、过载联锁、报警与复位。只生成结构，不生成代码。


10. CODESYS 皮带输送线分拣控制
实现一条皮带输送线的自动分拣控制：产品由入口光电检测到达后启动输送带；通过颜色/材质检测信号判定产品类别，分别驱动气缸 A 或气缸 B 将产品推入对应料道；未识别产品进入废料道。包含输送带堵料检测、推料气缸动作超时、计数统计、启停自保持、急停联锁、故障报警与复位。

要求以 json 文本输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染。json 需包含入口/分拣/到位传感器、颜色或类别信号、气缸前后位、启动停止复位急停、输送带和电磁阀输出、各类别计数器、延时与超时定时器、堵料/气缸故障状态及报警逻辑。只生成结构，不生成 PLC 代码。

11. CODESYS 星三角降压启动控制
实现一台大功率异步电机的星三角降压启动控制：按下启动后先吸合主接触器和星形接触器；星形启动延时结束后断开星形接触器，经过切换死区延时后吸合三角形接触器；运行中任意时刻停止、急停或热继电器过载均立即断开所有接触器并锁存故障。要求星形与三角形接触器必须具备电气互锁。

要求以 json 文本描述 IEC 61131-3 梯形图结构。json 需包含 Start/Stop/Reset/EStop/Motor_OL、主/星/三角接触器反馈、Main_Contactor/Star_Contactor/Delta_Contactor/Alarm 输出、启动阶段位、星形定时器、切换死区定时器、互锁触点、故障与复位梯级。只生成结构，不生成代码。

12. CODESYS 自动门双向控制
实现一套工业自动门控制：外侧和内侧各有开门按钮，开门到位后保持开启一段时间，再自动关门；关门过程中若安全光幕被遮挡或防夹边触发，应立即停止关门并重新开门；开到位和关到位均需停止对应输出；包含手动开/关模式、启停、急停、门机过载、开关门超时报警和故障复位。

要求以 json 文本输出梯形图结构，包含双侧按钮、安全光幕、防夹、开关到位、手动模式、门机过载、Open_Output/Close_Output、保持开启 TON、开门和关门超时 TON、方向互锁、故障报警和复位等变量与梯级。

13. CODESYS 恒压变频供水 PID 使能控制
实现两台变频泵恒压供水的上层控制逻辑：系统启动后根据压力低报警/压力达到设定值信号使能 PID 调节；先启动首选泵，若持续低压则投入第二台泵；压力恢复后按最小运行时间依次退出辅助泵；两泵按运行周期轮换首选；变频器故障、缺水保护或急停时退出运行并报警。

要求以 json 文本描述 IEC 61131-3 梯形图结构。json 需包括压力开关或 PID 状态输入、缺水、两台变频器故障、Pump1/2_Enable、PID_Enable、低压延时、最小运行时间、首选泵轮换位、辅助泵投退、故障跳过、报警与复位梯级。只生成结构，不生成算法代码。

14. CODESYS 包装机计数与批次停机控制
实现一台包装机批次生产控制：启动后包装机连续运行，产品光电每检测到一个包装完成品便计数；达到批次设定数量时停止设备并置位 Batch_Done；操作员确认并复位后开始下一批。若物料缺料、封口温度异常、传送带过载或急停，则立即停机并报警。支持手动清零批次计数。

要求输出 json 文本的 IEC 61131-3 梯形图结构，包含启停、确认、复位、清零、产品光电、缺料、温度异常、过载、急停输入；包装机输出、计数器 CTU、批次完成位、批次设定 INT、故障锁存、报警和复位梯级。只生成结构，不生成代码。

15. CODESYS 冷库压缩机除霜控制
实现一套冷库压缩机与电热除霜控制：在制冷模式下温度高于上限时启动压缩机，低于下限时停止；按设定间隔进入除霜模式，除霜时停止压缩机并接通除霜加热器；蒸发器温度达到除霜终止温度或最大除霜时间到达时退出除霜，并进入滴水延时，延时后恢复制冷。包含高压/低压保护、加热器过热保护、急停、报警与复位。

要求以 json 文本输出梯形图结构，需含温度上下限、除霜周期 TON、除霜最大时间 TON、滴水延时 TON、制冷/除霜/滴水阶段位、Compressor_Run、Defrost_Heater、保护输入、互锁、故障报警与复位梯级。只生成结构，不生成代码。

16. CODESYS 自动仓库堆垛机取放料控制
实现堆垛机单次取放料顺序控制：收到入库任务后，堆垛机横移至目标列、升降至目标层、货叉伸出取料或放料、货叉缩回、再回到安全原点；每个动作必须等待对应到位反馈，超时则停止全部驱动并报警。横移、升降和货叉动作需避免冲突，包含自动/手动模式、急停、伺服故障、任务完成和故障复位。

要求以 json 文本描述 IEC 61131-3 梯形图结构，包含任务开始、目标到位、横移/升降/货叉前后到位、原点、安全互锁、各动作输出、步骤 Step_1 至 Step_N、动作超时 TON、自动/手动模式、Alarm/Task_Done 和 Reset 梯级。只生成结构，不生成代码。

17. CODESYS 空压机群控与压力联锁
实现三台空压机群控：压力低于下限时逐台投入空压机，压力达到上限时按最小运行时间依次卸载；按累计运行时间轮换主机；若某台空压机过温、过载或变频器故障，则跳过该机并由可用机补位；压力超低时启动全部可用机并报警。包含系统启停、急停、维护锁定单台设备、故障确认复位。

要求以 json 文本输出梯形图结构，包含压力上下限/超低、三台设备可用状态和维护锁定、运行时间/轮换状态、最小运行时间 TON、Compressor1_Run 至 Compressor3_Run、逐级投切、故障跳过、报警和复位梯级。只生成结构，不生成代码。

18. CODESYS 两工位冲压机安全循环控制
实现两工位冲压机控制：双手启动按钮必须在规定同步时间内同时按下，冲压滑块才允许下行；下死点到位后自动回程至上死点；安全门打开、光幕遮挡、急停、液压低压或模具保护信号触发时禁止下行并报警；支持单次/连续两种模式，连续模式每次循环仍需满足安全条件。

要求以 json 文本描述 IEC 61131-3 梯形图结构，包含双手按钮同步 TON、安全门/光幕、上下死点、液压压力、单次连续模式、Slide_Down/Slide_Up 输出、循环状态位、下行和回程超时、故障锁存、报警及复位梯级。只生成结构，不生成代码。

19. CODESYS 反渗透 RO 自动制水与反冲洗控制
实现反渗透制水系统控制：原水箱液位满足且产水箱低液位时，依次启动原水泵、高压泵和产水阀；产水箱高液位时停止制水。按累计制水时间或压差高报警触发自动反冲洗，反冲洗时关闭产水阀、切换冲洗阀并运行冲洗泵，到达冲洗时间后恢复制水。包含原水低液位保护、高压泵过载、低压/高压开关保护、电导率超限排放、急停、报警及复位。

要求以 json 文本输出 IEC 61131-3 梯形图结构，需包含各液位、压差、压力、电导率、过载与急停输入；原水泵/高压泵/冲洗泵/产水阀/冲洗阀输出；制水和反冲洗阶段位、累计时间与反冲洗 TON、联锁、故障报警和复位梯级。只生成结构，不生成代码。