1. CODESYS 三泵水箱自动补水 + 轮换控制

实现一个在 CODESYS V3 上运行的三台补水泵自动控制与轮换逻辑（Pump1、Pump2、Pump3），包含系统启停、自保持、液位高/低/超高判断、急停与过载联锁、最小运行时间和主备轮换策略等。

请以 **json 文本** 的方式，输出符合 IEC 61131-3 标准的 **梯形图（Ladder Diagram）控制逻辑结构**，用于后续渲染成梯形图，而不是生成 ST 代码。需要在 json 中明确：I/O 点（输入/输出标签）、内部位和定时器、各梯级（rung）的名称以及触点/线圈的顺序与类型（NO/NC、LATCH、SET/RESET 等）。

---

2. CODESYS 双水泵交替运行控制

实现一个在 CODESYS V3 上运行的双泵供水系统控制逻辑，支持：系统启停、急停联锁、单泵过载保护、两泵交替运行（一次用泵1，下次用泵2），水箱液位高/低控制启停，以及故障跳过某台泵的逻辑。

要求以 **json 文本** 输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染成梯形图。json 中需包含：输入/输出信号列表、内部轮换状态位、各梯级的条件（启停自保持、轮换选择、过载联锁、液位控制等），以及对应的触点/线圈类型和排列。不要生成具体 PLC 代码，只生成可视化用的梯形图描述。

---

3. CODESYS 恒压供水单泵 PID 控制

实现一个在 CODESYS V3 上运行的恒压供水系统，控制一台变频水泵：系统启停、自保持、急停联锁、过载保护，基于压力反馈和设定值的 PID 控制输出频率指令，以及故障报警与复位逻辑。

请以 **json 文本** 的方式，描述 IEC 61131-3 梯形图的结构，用于后续渲染梯形图。json 中应该包括：I/O 标签（按钮、急停、压力反馈、过载、模拟量输出等），内部 PID 相关位（例如运行允许位、故障位、报警位），以及各梯级的触点/线圈组合，说明 PID 运行启停条件、故障联锁与报警复位。无需给出 PID 算法细节代码，只需梯形图级别的控制逻辑。

---

4. CODESYS 双工位分拣输送线控制

实现一条主输送线 + 左右两个工位的分拣控制逻辑：系统启停、急停联锁、主输送线运行与到位暂停、根据外部选择信号（Select_Left）决定分拣方向，通过气缸推杆分拣到左/右工位，气缸伸出/缩回到位反馈，故障与报警逻辑，以及左右工位计数功能。

要求以 **json 文本** 输出 IEC 61131-3 梯形图结构，用于后续渲染。json 需包含：所有输入/输出点（启动、停止、急停、光电、气缸反馈、选择信号等），内部请求位（Sort_Request）、计数器标签（Count_Left/Count_Right）以及每个梯级的触点/线圈配置（例如输送线自保持、分拣请求置位、气缸动作控制、计数逻辑、报警联锁等）。

---

5. CODESYS 轨道小车自动/手动双模式控制

实现一个轨道小车（Transfer Cart）的双模式控制：自动模式下在左/右端之间按节拍往返；手动模式下通过 Jog 信号点动前后移动；包含系统启停、急停联锁、左右端到位检测、驱动过载保护、行程超时报警，以及自动/手动模式切换互锁。

请以 **json 文本** 的方式，输出用于 IEC 61131-3 梯形图渲染的逻辑结构。json 中应包括：所有输入信号（模式开关、启停按钮、端位开关、过载、JOG 命令等）、输出信号（Cart_Move_Left/Right、Alarm）、内部状态位（例如 Cart_Run、Auto_Mode、Manual_Mode、Fault）、以及每个梯级的条件和触点/线圈排列，用于表示启停自保持、模式切换、运动方向控制、故障联锁与报警复位。不要输出 ST 或 IL 代码，只给结构化的梯形图描述。

---

6. CODESYS 配料罐加热 + 搅拌工艺控制

实现一个配料罐的自动工艺控制：液位达到工艺要求后，开启加热器和搅拌电机；按温度设定值控制加热启停；存在防超温逻辑（超过上限温度强制停机并报警）；在达到设定温度后持续搅拌一段设定时间；包含系统启停、急停联锁、加热和搅拌过载保护、报警与复位逻辑。

请以 **json 文本** 描述符合 IEC 61131-3 的梯形图控制逻辑，用于后续渲染成梯形图。json 需包含：输入点（启停、复位、急停、液位、温度反馈、过载信号等）、输出点（Heater_Enable、Mixer_Run、Alarm）、内部阶段/定时器位（加热阶段、搅拌阶段、防超温状态等），以及每个梯级的触点与线圈配置，用来表达工艺顺序、温度与液位条件、故障联锁和报警复位。只需结构，不需具体执行代码。

---

7. CODESYS 四电机顺序启动控制

实现一个四台电机（M1~M4）的顺序启动控制系统：在系统启动后按顺序 M1→M2→M3→M4 依次启动，每台之间有固定延时；任何时刻按下停止或发生急停/过载故障时立即停机并锁存故障；提供复位按钮清除故障后可重新启动整个顺序。

要求以 **json 文本** 输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染。json 应列出：所有输入信号（Start/Stop/Reset/Estop/各电机过载等）、输出信号（M1_Run~M4_Run、Alarm）、内部顺序状态位或步进位（例如 Step_1~Step_4）、定时器标签，以及每个梯级的触点/线圈组合，用来表示自保持启停、按步推进顺序启动、过载联锁和报警复位。不要生成具体 PLC 程序，只生成梯形图结构描述。

---

8. CODESYS 单路口三色交通灯定时控制

实现一个单路口三色交通灯控制逻辑：绿灯→黄灯→红灯按固定周期循环；系统启停和急停联锁；通过定时器控制每个灯的亮灯时长，并在周期结束后重新回到绿灯阶段。

请以 **json 文本** 的方式，输出 IEC 61131-3 梯形图逻辑结构，用于后续渲染成梯形图。json 中应包含：输入信号（Start/Stop/Reset/Estop）、输出信号（Red_Lamp、Yellow_Lamp、Green_Lamp）、定时器标签（T_Green、T_Yellow、T_Red），以及每个梯级的触点和线圈（启停自保持、各灯阶段的点亮条件、定时器启停、周期复位逻辑等）。只需结构化描述，不需要具体代码实现。

---

9. CODESYS 三泵污水提升泵站自动控制 + 轮换

实现一个污水提升泵站控制系统，包含 3 台提升泵（Pump1~Pump3）：根据液位低/高/超高信号控制启停；高液位时启动主泵；超高液位时启动第二台泵并报警；具有主泵轮换（按时间或运行周期轮换 Pump1/Pump2/Pump3）、单泵过载故障跳过、系统启停和急停联锁、故障报警与复位逻辑。

请以 **json 文本** 输出符合 IEC 61131-3 的梯形图结构，用于后续渲染。json 需列出：输入信号（Start/Stop/Reset/Estop/Level_Low/High/HH 以及 Pump1_OL~Pump3_OL）、输出信号（Pump1_Run~Pump3_Run、Alarm）、内部轮换状态位（Current_Lead_Pump）、定时器（Min_Run_Time、Rotation_Interval），以及每个梯级的触点和线圈配置，用来表达液位条件启停、轮换选择、过载联锁、报警与复位。只生成结构，不生成代码。